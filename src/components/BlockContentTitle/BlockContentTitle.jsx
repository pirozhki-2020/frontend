import React from 'react';

import './BlockContentTitle.scss';

const BlockContentTitle = ({children}) => {
    return <div className="content_title">{children}</div>;
};

export default BlockContentTitle;
